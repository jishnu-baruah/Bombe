{
  "name": "bombe-subgraph",
  "version": "0.1.0",
  "private": true,
  "description": "The Graph subgraph for the Bombe AgentAttestation contract on Mantle Sepolia (the scaling path for the /explorer feed). Not deployed here.",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.80.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  }
}
