{
  "name": "bombe",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "//ci": "D7: the ci script (invoke as `pnpm run ci`) is incremental. T-102 added contracts:test to the gate (forge test). vitest and `pnpm run test:demo` are appended as later milestones land.",
    "ci": "pnpm lint && pnpm typecheck && pnpm contracts:build && pnpm contracts:test",
    "contracts:build": "forge build --root contracts",
    "contracts:test": "forge test --root contracts",
    "test": "echo \"T-7xx: test harness not implemented yet\" && exit 1",
    "test:agent": "echo \"T-702: test:agent aggregator not implemented yet\" && exit 1",
    "test:demo": "echo \"T-703: test:demo golden path not implemented yet\" && exit 1",
    "demo": "echo \"M3+: demo boot not implemented yet\" && exit 1",
    "deploy:testnet": "echo \"T-804: deploy:testnet not implemented yet\" && exit 1"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9"
  }
}
