{
  "name": "@bombe/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server exposing Bombe's read/verify/request tools so agents can use the attestor network headlessly.",
  "bin": {
    "bombe-mcp": "./src/index.ts"
  },
  "scripts": {
    "start": "node --import tsx ./src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  }
}
