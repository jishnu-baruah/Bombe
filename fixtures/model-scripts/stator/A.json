{
  "agentId": "stator",
  "claimId": "A",
  "steps": [
    {
      "thought": "Fetching mETH yield — single source check for cost efficiency.",
      "action": { "tool": "fetch_meth_yield", "input": { "period": "30d-fresh" } }
    },
    {
      "thought": "34 bps, fresh data, matches claim. Finalizing VALID immediately.",
      "action": {
        "finalize": {
          "decision": "VALID",
          "confidenceBps": 9000,
          "rationaleSummary": "Fresh oracle confirms mETH yield is 34 bps, matching expected value within tolerance."
        }
      }
    }
  ]
}
